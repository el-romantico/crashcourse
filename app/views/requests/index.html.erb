<div class="row">
  <div class="col-sm-3 col-md-2 sidebar">
    <ul class="nav nav-sidebar">
      <% @tags.each do |tag| %>
        <li><%= link_to tag, '/requests/' + tag %></li>
      <% end %>
    </ul>
  </div>

  <div id="js-requests-list" class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
    <%= render 'courses/tags', {requests: @requests} %>
  </div>
</div>